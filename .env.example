# Backend API configuration
# Default backend URL (no trailing slash)
API_BASE=http://localhost:3000

# Bearer token for message endpoints
API_TOKEN=super-secret-doodle-token

# Frontend dev port (this project uses 3001 by default)
# Note: changing this is optional; the dev script uses 3001 already.
NEXT_PUBLIC_PORT=3001
